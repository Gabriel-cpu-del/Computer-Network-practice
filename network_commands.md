# Network Commands

## ping

The `ping` command checks whether another device or server is reachable.

Example:

```bash
ping google.com

  IP address
The ip command shows network configuration.
  ## DNS

DNS translates domain names into IP addresses.

Example:

```bash
nslookup google.com
